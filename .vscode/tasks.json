{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "rpfm_cli build",
            "type": "cargo",
            "subcommand": "build",
            "problemMatcher": [
                "$rustc"
            ],
            "options": {
                "cwd": "${workspaceFolder}/rpfm_cli",
            }
        }
    ]
}
